# 未決事項・確認依頼

[要件定義書](01-requirements.md) を確定させるために回答が必要な項目。
優先度 **高** は設計着手前に、**中** は Phase 1 実装中に、**低** は該当フェーズ着手前に確定させる。

> Q 番号は議論の継続性のため、解決済みの項目の番号を再利用しない。
> Q-1〜Q-3 は解決済みのため下記「確定済みの前提」に移した。

---

## 確定済みの前提

| # | 項目 | 決定 | 決定日 |
| --- | --- | --- | --- |
| D-1 | 計測の実行方式 | **CI 成果物の取り込み型**。quality-gate はテスト・スキャンを自前実行しない | 2026-09-21 |
| D-2 | 利用形態 | **社内専用・単一テナント**。マルチテナント / 課金は要件外 | 2026-09-21 |
| D-3 | 技術スタック | **Spring Boot 4 + Vue 3**（計測対象と同一構成、ドッグフーディング前提） | 2026-09-21 |
| D-4 | 判定結果の運用 | **レポート・可視化のみ**。PR のマージブロックは行わない | 2026-09-21 |
| D-5 | ミューテーションテストのツール | **PIT（pitest）を利用**。JVM 専用ツールのため、M-02 の対象は **backend のみ**となる（派生する確認事項: 下記 Q-11） | 2026-09-21 |
| D-6 | スループットの合格ライン（旧 Q-1） | スループットを結果として測るのではなく、**到達率 50 req/s を負荷条件として固定**し、その条件下で p95 ≤ 500ms・エラー率 ≤ 0.1% を判定する。50 req/s は初期値とし、導入初回の計測で妥当性を確認する | 2026-09-21 |
| D-7 | 性能計測用の専有環境（旧 Q-2） | **確保する**。専有の自ホスト型ランナー上で計測し、絶対値しきい値（p95 500ms）で判定する | 2026-09-21 |
| D-8 | しきい値の適用方式（旧 Q-3） | 対象リポジトリはベースラインが整っており、かつマージをブロックしないため、**ラチェット方式は採用しない**。本仕様の絶対値しきい値を当初から適用する | 2026-09-21 |

D-8 に伴い、[指標仕様書 付録 A](02-metrics-spec.md) は
「移行期のラチェット運用」から「導入手順」へ差し替えた。
段階的なしきい値の引き上げは行わず、導入初期の期間は
**計測そのものが信頼できるかを確認するための期間**と位置づけている。

---

## Q-4. 対象リポジトリは何個か、構成はモノレポかポリレポか【優先度: 中】

要件定義書では「初期 5 / 3 年後 50」を想定値として置いています。

- Spring Boot と Vue が同一リポジトリ（モノレポ）か、別リポジトリ（ポリレポ）か
- ポリレポの場合、「フロントとバックで 1 つの判定」にまとめる必要があるか、
  それぞれ独立に判定してよいか

ポリレポで統合判定が必要な場合、複数リポジトリの Run を束ねる
「プロダクト」という上位概念をデータモデルに追加する必要があります。

---

## Q-5. 認証に使う IdP は何か【優先度: 中】

OIDC 対応を前提に設計していますが、具体的な IdP により実装の細部が変わります。

- Microsoft Entra ID / Google Workspace / Okta / Keycloak / GitHub OAuth など
- グループ情報（ロールのマッピング元）をどの claim で受け取れるか
- ロールを IdP のグループから自動マッピングするか、quality-gate 内で個別に付与するか

---

## Q-6. ミューテーションテストの CI 実行時間をどこまで許容できるか【優先度: 中】

要件定義書では既定を「変更クラスのみ（`scope: changed`）」としていますが、
それでも PIT は相応の時間を要します（全量では単体テストの数十倍）。

- PR の CI 全体の許容時間（例: 15 分以内）
- 変更クラス限定でも超過する場合、ミューテーションテストを
  PR ではなく main への push 後（または夜間バッチ）のみに回すか

**推奨**: PR では差分限定、main への push 後に全量を夜間実行。
PR の待ち時間を増やさずに、両方の値を得られます。

---

## Q-7. 通知先と通知条件の運用【優先度: 中】

- Slack のチャネルはリポジトリごとに分けるか、1 つに集約するか
- 「合格→不合格の遷移時のみ」を既定としていますが、これで妥当か
- 不合格が継続している場合の再通知（毎日 / 週次 / しない）をどうするか

通知が多すぎると無視され、少なすぎると気づかれません。
**既定は「遷移時のみ + 週次サマリ」**を推奨します。

---

## Q-8. 不合格時の是正プロセスをどう定めるか【優先度: 中】

D-4 のとおり本フェーズではマージをブロックしないため、
**不合格が放置されない運用ルール**が別途必要です。これはアプリの機能ではなく
運用の取り決めですが、これを欠くと [リスク R-7](01-requirements.md#13-リスクと対策)
のとおり「可視化しただけで何も改善しない」状態になります。

決めていただきたいこと:

- 不合格を検知してから是正までの目標期間（例: Critical 脆弱性は 3 営業日以内）
- 週次で不合格リポジトリをレビューする場（既存の定例に組み込めるか）
- Phase 3（マージブロック）へ移行する判断基準と時期の目安

---

## Q-9. 成果物の保持期間とストレージ【優先度: 低】

要件定義書では「Run メタデータ・指標値 2 年 / 元成果物 90 日」を既定としています。

- 監査・リリース判定の観点で、より長期の保持が必要か
- オブジェクトストレージ（MinIO 等）を新規に立てられるか、既存の S3 互換基盤があるか

---

## Q-10. デプロイ先と運用体制【優先度: 低】

- オンプレミスか、プライベートクラウド（AWS / Azure / GCP）か
- Docker Compose での単一ホスト運用で足りるか、Kubernetes が前提か
- 運用担当（監視・バックアップ・アップデート）はどのチームか

社内単一テナント・初期 5 リポジトリの規模であれば、
**Docker Compose による単一ホスト構成**で十分と考えています。

---

## Q-11. frontend のミューテーションテストを対象外とすることでよいか【優先度: 低】

D-5（PIT の採用）から導かれる確認事項です。PIT は JVM 専用ツールのため、
**Vue 3 / TypeScript 側のミューテーションスコアは計測されません**。

結果として、frontend のテストの実効性を検証する手段は
M-01 ブランチカバレッジのみとなります。カバレッジはアサーションの有無を見ないため、
「コードを通しているだけのテスト」を見破れない点が残ります。

| 選択肢 | 内容 |
| --- | --- |
| A（現在の前提） | frontend は M-01 のみで担保する。M-02 は backend 限定 |
| B | frontend にも Stryker Mutator を導入し、M-02 を両コンポーネントで計測する |

A で問題ないという理解で進めています。Vue 側のロジックが薄く（表示中心で、
業務ロジックがバックエンドに寄っている）構成であれば A で妥当です。
フロント側に複雑な状態管理や計算ロジックがある場合は、B をご検討ください。

---

## 次のステップ

1. Q-4（リポジトリ構成）/ Q-5（IdP）にご回答いただく — 基本設計の前提になります
2. Q-6〜Q-8（実行時間・通知・是正プロセス）は Phase 1 実装中に確定させる
3. 回答を反映して要件定義書を v1.0 として確定する
4. 確定後、基本設計（画面設計・API 設計・データベース設計）へ進む
