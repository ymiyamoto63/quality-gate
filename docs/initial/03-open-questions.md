# 決定事項の記録と残課題

[要件定義書](01-requirements.md) は **v1.0 として確定済み**（2026-09-21）。
本書は、その確定に至った決定事項の記録と、確定を妨げない残件の一覧である。

残件は優先度 **中** が Phase 1 実装中、**低** が該当フェーズ着手前の確定となる。
**優先度「高」の未決事項は残っていない。**

> Q 番号は議論の継続性のため、解決済みの項目の番号を再利用しない。
> Q-1〜Q-5、Q-11〜Q-13、Q-15 は解決済みのため下記「確定済みの前提」に移した。

---

## 確定済みの前提

| # | 項目 | 決定 | 決定日 |
| --- | --- | --- | --- |
| D-1 | 計測の実行方式 | **CI 成果物の取り込み型**。quality-gate（バックエンド）はテスト・スキャンを自前実行しない。**計測を誰が行うかは D-16 で改訂**（バックエンドが取り込みと判定だけを行う点は変わらない） | 2026-09-21（2026-09-23 改訂） |
| D-2 | 利用形態 | **社内専用・単一テナント**。マルチテナント / 課金は要件外 | 2026-09-21 |
| D-3 | 技術スタック | **Spring Boot 4 + Vue 3**（計測対象と同一構成、ドッグフーディング前提） | 2026-09-21 |
| D-4 | 判定結果の運用 | **レポート・可視化のみ**。PR のマージブロックは行わない | 2026-09-21 |
| D-5 | ミューテーションテストのツール | **PIT（pitest）を利用**。JVM 専用ツールのため、M-02 の対象は **backend のみ**となる（frontend を対象外とする点は D-9 で確定） | 2026-09-21 |
| D-6 | スループットの合格ライン（旧 Q-1） | スループットを結果として測るのではなく、**到達率 50 req/s を負荷条件として固定**し、その条件下で p95 ≤ 500ms・エラー率 ≤ 0.1% を判定する。50 req/s は初期値とし、導入初回の計測で妥当性を確認する | 2026-09-21 |
| D-7 | 性能計測用の専有環境（旧 Q-2） | **確保する**。専有の自ホスト型ランナー上で計測し、絶対値しきい値（p95 500ms）で判定する | 2026-09-21 |
| D-8 | しきい値の適用方式（旧 Q-3） | 対象リポジトリはベースラインが整っており、かつマージをブロックしないため、**ラチェット方式は採用しない**。本仕様の絶対値しきい値を当初から適用する | 2026-09-21 |
| D-9 | frontend のミューテーションテスト（旧 Q-11） | **対象外**。業務ロジックがバックエンドに寄っているため、frontend は M-01 ブランチカバレッジで担保する | 2026-09-21 |
| D-10 | 対象リポジトリの構成（旧 Q-4） | **モノレポ 1 リポジトリ**（`backend/` + `frontend/`）。複数リポジトリを束ねる「プロダクト」概念は不要 | 2026-09-21 |
| D-11 | 認証方式（旧 Q-5） | **GitHub OAuth**（個人アカウント / GitHub Free）。単一の GitHub App の user-to-server 認可フローでログインとリポジトリ読み取りの双方を賄う。Organization を持たないため、ログイン可否は quality-gate 内の**許可リスト**で制御する | 2026-09-21 |
| D-12 | ロール構成と免除フロー（旧 Q-13） | **Phase 1 は Admin / Viewer の 2 ロール**、（免除は D-22 で廃止）免除は Admin による登録で即時有効（承認フローなし）。理由・期限・実施者の記録は必須。利用者が増えた時点で 4 ロール + 承認フローへ拡張する | 2026-09-21 |
| D-13 | ランナー種別と重量ジョブの実行可否（旧 Q-12） | **ワークフローで切り替え可能**とする。ランナー種別（`self-hosted` / GitHub ホスト）をリポジトリ変数と `workflow_dispatch` 入力で切り替え、GitHub ホスト時は PIT / k6 を実行するかスキップするかを選べる。スキップは**申告制**とし、未提出（ERROR）と区別する。GitHub ホストでの性能計測値は判定に用いず参考値（`REFERENCE`）とする。**収集ランナー（D-16）は常にセルフホストランナーで動くため、切り替えの対象外**（quality-gate 自身の CI にだけ適用する。**D-18 で自身の計測を廃止したため、実装したワークフローは無い**）。**D-19 でランナー種別（`runnerType`）と `reference_only_environments` を削除した。スキップの申告制は収集ランナーが使うため残す** | 2026-09-21 |
| D-14 | 技術スタック | **Maven / Java 25 LTS / SPA は Spring Boot 同梱（同一オリジン）/ openapi-typescript + openapi-fetch**。成果物ストレージは MinIO ではなくローカルファイルシステム。詳細は [04-tech-stack.md](04-tech-stack.md) | 2026-09-21 |
| D-15 | 通知チャネル（旧 Q-7 の一部） | **メールのみ**。Slack（Incoming Webhook）と GitHub PR コメントは運用上不要と判断し、実装から外した（FR-11-3 / FR-11-4 を改訂）。通知条件は「合格 → 不合格の遷移時のみ」を既定とし、リポジトリごとに毎回 / 不合格時のみ / 無効を選べる | 2026-09-23 |
| D-16 | 計測の実行場所（旧 Q-15。D-1 の改訂） | **quality-gate 側の「収集ランナー」が対象リポジトリを取得して計測し、Ingest API に送る**。対象リポジトリには設定ファイル・ワークフロー・計測用の依存関係を置かない。収集ランナーは quality-gate リポジトリの GitHub Actions（セルフホストランナー）で動き、GitHub App（Contents / Pull requests の読み取り）で対象を読む。合格ラインは画面（S-06）で管理する。バックエンドはテストを実行しない（D-1 の核は維持）。対象の CI から Ingest API へ送る従来の方式も引き続き受け付ける。経緯と移行計画は [収集ランナー方式](../architecture/collector-runner.md) | 2026-09-23 |
| D-17 | quality-gate 自身の性能計測（A-7 の改訂） | **計測しない**。quality-gate は少人数で使う社内ツールで、到達率 50 req/s の負荷試験は実際の使われ方とかけ離れており、性能の保証にならない。負荷をかける先が本番と同じになりやすく、レート制限の解除とセッション Cookie の貼り直しが要るなど運用の手間も大きい。ドッグフーディング（10.7 / A-7）は**性能（M-03〜05）を除く全指標**とする。M-03〜05 の判定機能（k6 / Gatling の成果物の読み取りと判定）は他の対象のために残す。非機能要件 10.1 の性能目標は自動の負荷試験では検証しない | 2026-09-24 |
| D-18 | quality-gate 自身の計測（A-7 の改訂） | **計測しない**。quality-gate 自身を計測して送るワークフロー（`quality-gate.yml`）を廃止する。対象リポジトリの計測は収集ランナーに移っており（D-16）、自身の計測だけが別の経路で残っていた。main への push のたびにセルフホストランナーを使うため、収集ランナーの性能計測と同じマシンで動いてしまう（D-7 の「他のジョブと同居させない」に反する）。指標を計測する代わりに、Pull Request の CI（`ci.yml`）で結合テスト・生成物の同期検証・アクセシビリティ検査（axe-core の critical / serious 0 件）・脆弱性スキャン（修正版のある重大・高 0 件）を実行し、失敗すればマージしない。PIT（M-02）・複雑度（M-07）・破壊的変更（M-09）は自身には適用しない。自身の指標を画面で見たくなった場合は、収集ランナーの対象に登録する。`.quality-gate.yml` は CI から直接送る方式の設定例として残す。D-13 のランナー種別の切り替えを実装したワークフローは無くなる（7.1.1 のサンプルは対象の CI 向けに残す） | 2026-09-24 |
| D-19 | 取り込み経路の一本化（D-16 の続き） | **収集ランナーに絞る**。対象リポジトリの計測は収集ランナーだけが行っており、対象の CI から送る経路は使われていなかった。次のものを削除する: 対象の CI から送るための composite action（`quality-gate-action/`）と CLI（`cli/qg-submit`）、設定例の `.quality-gate.yml`、ランナー種別（`runnerType`。D-13）と `execution.reference_only_environments`（収集ランナーは常に専有のセルフホストランナーで動く）、Check Run の出力（`enforcement`。使っている対象が無く、D-4 ではマージを止めない）、収集ランナーが送らない成果物の形式（istanbul-json / gatling-log / osv-json / lizard-csv / pact-verification）。保存済みの設定からは V016 で `enforcement` と `reference_only_environments` を取り除く。Ingest API と、設定ファイルを成果物（`quality-gate-config`）として受け取る仕組みは残す | 2026-09-26 |
| D-20 | 合格ラインの置き場所（D-16 の続き） | **quality-gate リポジトリの `collector/targets/<owner>__<name>.gate.yml` だけにする**。収集ランナーが Run ごとに `quality-gate-config` として送り、quality-gate はその内容で判定する。それまでは画面（S-06）で保存した設定で判定し、ファイルは「貼り付ける控え」だったため、合格ラインが 2 か所に分かれ、ずれる余地があった。画面からの編集（FR-02-3）と、編集中の設定で過去の Run を判定し直すドライラン（FR-02-5）は削除する（変更はプルリクエストでレビューし、Git の履歴で追う）。設定ファイルの無い Run（D-20 より前の Run）を再評価するときは、前回の判定で使った版を使う。`*.gate.yml` が無ければ収集ランナーは送信の前に止まる | 2026-09-26 |
| D-21 | 周辺機能の削除（バッジ・レート制限） | **どちらも削除する**。バッジ（FR-08-5）は README に貼った画像を GitHub の画像プロキシがインターネット側から取りに行くため、インターネットに公開しない構成（C-7）では表示できず、機能として成立していなかった。同じ情報（既定ブランチの最新の合否）はダッシュボードで見られる。認証なしで開いていた唯一の入口もなくなる。レート制限は CI の暴走や総当たりへの歯止めだったが、取り込み元はトークンで認証した収集ランナー 1 台（D-19）、参照するのは許可リストの少人数で、上限に届く使い方が無い。収集ランナーの `curl --retry` は一時的な障害（5xx など）の再試行として残す | 2026-09-26 |
| D-23 | リリース判定（UC-10） | **タグかコミットを指定し、そのコミットで判定済みの Run から結論を出す**。結論は「リリース可」「リリース可（注意あり）」「リリース不可」「判定できない」の 4 つ。使うのは同じコミットの最新の完全計測で、無ければ最新の Run。近くのコミットでは代用しない（別のコードの結果になる）。測った範囲に不合格があれば部分計測でも「不可」、部分計測で不合格が無ければ「判定できない」（測っていない指標を合格とみなさない）。判定し直しはせず、Run の判定時の結果をそのまま使う（見るたびに変わると証跡にならない）。免除は D-22 で廃止したため一覧は出さず、合格ラインの版と除外パターンを示す。経営陣や開発初心者にも読めるよう、指標ごとに説明と基準の根拠を出し、根拠の強さを「外部基準 / 業界の目安 / チーム判断」で明示する（全部を同じ口調で書くと、チームで決めた値まで外部の基準に見える）。根拠の文は既定値の説明で、基準を変えた理由は合格ラインのコミットに残す（`.gate.yml` に理由の欄は設けない）。CSV は 1 行 1 指標の平らな表とし、出力を監査ログに残す。PDF はブラウザの印刷。前回リリースとの比較・承認の仕組み・総合点は入れない | 2026-09-26 |
| D-24 | リリース判定のための計測 | **タグを指定して計測でき、比較元は前のタグにする。PIT と負荷試験は PR 以外のすべての計測で実行する**。従来は PIT と負荷試験を既定ブランチでだけ実行していたため、リリースブランチやタグの計測は部分計測になり、リリース判定が必ず「判定できない」になっていた（定期実行の廃止（D-22）で、実行回数の多さという当初の理由も無くなった）。また比較元が直前のコミットだったため、新規の違反・破壊的変更・スキップの増加を最後の 1 コミット分しか見ておらず、途中のコミットで入ったものを見逃していた。収集ランナーはタグ（`commit` 入力）と比較元（`base` 入力。コミットかタグ）を受け付け、タグの計測では `git describe --tags` で前のタグを比較元にする。quality-gate は比較元コミットで判定済みの Run を比較対象 Run に優先して使う（手動の計測は順不同で、同じブランチで直前に計測した Run がリリースより新しいこともあるため）。リリース判定の画面と CSV には比較元のコミットを出す | 2026-09-26 |
| D-25 | 重複した定義と指標の整理 | **コンポーネントは計測プロファイルだけで決め、M-08 API 契約テスト成功率を廃止し、比較元は収集ランナーだけが求める**。コンポーネントの定義が 3 か所（設定ファイルの `components`、DB と管理画面、計測プロファイルの `BACKEND_DIR` など）にあったが、計測を分けているのは計測プロファイルだけで、DB の定義は画面に表示するだけ、設定ファイルの `components` は読まれていなかった。判定と表示には成果物に付いたコンポーネント名を使う。M-08 の入力（`*ControllerTest` の JUnit XML）は M-11 の入力の一部で、同じテストの失敗を 2 つの指標で数えていた（収集ランナーはテストの種類を見分けられず、consumer と provider の間の契約を検証するテストでもなかった）。テストの成功は M-11（既定で有効に変更）、API の互換性は M-09 で見る。過去の Run の M-08 は削除する（V019）。比較元（`baseCommitSha`）が省略された Run の比較元を GitHub API で求める処理は、収集ランナーが常に比較元を送るため削除した。設定ファイルで受け付けるだけで判定に使っていなかった項目（`on_missing_report`、M-01 / M-02 / M-07 の `scope`、M-01 の `per_component`、M-06 の `max_medium`）は、書いても結果が変わらず効いていると誤解させるため削除した。M-01 の `diff_threshold` は「変更行の分岐カバレッジ」と説明していたが、実装は全体値の注意ライン（これ未満は WARN）だったため `warn_below` に改めた（変更行のカバレッジは実装していない） | 2026-09-26 |
| D-22 | 免除・通知・定期実行の廃止 | **3 つとも廃止し、計測は収集ランナーの手動実行だけにする**。運用しておらず、仕組みの割に使われていなかった。免除（FR-10）の代わりは合格ライン（`*.gate.yml`）の `exclusions`・指標の `enabled`・しきい値の変更で、理由と実施者はコミットに残る（D-20）。メール通知（FR-11）の代わりは画面での確認。定期実行は収集ランナーの 15 分ごとの計測（`detect.sh` と計測済みの記録 `state.sh`）とサーバの日次バッチのうち再評価・免除の期限切れ・鮮度確認を削除する。日次再評価は保存済みの成果物と Run とともに送られた設定で判定し直すだけで、新しい CVE やしきい値の変更は拾えず、判定が変わる原因は免除の増減だけだった。手動実行では計測間隔が空くのが普通になるため、計測途絶と完全計測途絶の警告（FR-06-2 / FR-06-3）も外し、最終計測と最後の完全計測の日時の表示だけを残す。保持期間の削除と、滞留した Run の後始末の日次バッチは残す。Q-7 は解消 | 2026-09-26 |

D-8 に伴い、[指標仕様書 付録 A](02-metrics-spec.md) は
「移行期のラチェット運用」から「導入手順」へ差し替えた。
段階的なしきい値の引き上げは行わず、導入初期の期間は
**計測そのものが信頼できるかを確認するための期間**と位置づけている。

---

## Q-6. ミューテーションテストの CI 実行時間をどこまで許容できるか【優先度: 中】

要件定義書では既定を「変更クラスのみ（`scope: changed`）」としていますが、
それでも PIT は相応の時間を要します（全量では単体テストの数十倍）。

- PR の CI 全体の許容時間（例: 15 分以内）
- 変更クラス限定でも超過する場合、ミューテーションテストを
  PR ではなく main への push 後（または夜間バッチ）のみに回すか

**推奨**: PR では差分限定、main への push 後に全量を夜間実行。
PR の待ち時間を増やさずに、両方の値を得られます。

収集ランナー（D-16）では対象の CI の実行時間に影響しないため、論点は「セルフホストランナーをどれだけ占有してよいか」に変わる。
M-02 を収集ランナーへ移す段階 3 では、既定ブランチのみに絞り、全量を新しいコミットごとに実行することにした（like-chatgpt で約 1 分）。
PR の Run では M-02 のスキップを申告する。ランナーの占有が問題になったら夜間に絞る（[収集ランナー方式](../architecture/collector-runner.md) 7 章）。

---

## Q-7. 通知先と通知条件の運用【解消。D-22 で通知を廃止】

- ~~通知先の Slack チャネル~~ → 通知はメールのみと決定（D-15）。宛先はリポジトリごとに画面で設定する
- 「合格→不合格の遷移時のみ」を既定としていますが、これで妥当か（実装の既定はこれ。D-15）
- 不合格が継続している場合の再通知（毎日 / 週次 / しない）をどうするか

通知が多すぎると無視され、少なすぎると気づかれません。
**既定は「遷移時のみ + 週次サマリ」**を推奨します。

---

## Q-8. 不合格時の是正プロセスをどう定めるか【優先度: 中】

D-4 のとおり本フェーズではマージをブロックしないため、
**不合格が放置されない運用ルール**が別途必要です。これはアプリの機能ではなく
運用の取り決めですが、これを欠くと [リスク R-7](01-requirements.md#13-リスクと対策)
のとおり「可視化しただけで何も改善しない」状態になります。

決めていただきたいこと:

- 不合格を検知してから是正までの目標期間（例: Critical 脆弱性は 3 営業日以内）
- 週次で不合格リポジトリをレビューする場（既存の定例に組み込めるか）
- Phase 3（マージブロック）へ移行する判断基準と時期の目安

---

## Q-9. 成果物の保持期間とストレージ【優先度: 低】

要件定義書では「Run メタデータ・指標値 2 年 / 元成果物 90 日」を既定としています。

- 監査・リリース判定の観点で、より長期の保持が必要か
- 保存先はローカルファイルシステム（バインドマウント）で確定済み（[04](04-tech-stack.md) 5.2）。
  必要な空き容量（初期 10GB 程度）を確保できるか

保持日数は上記を既定値として、管理画面（S-09）から変更できるよう実装済み。残るのは運用上の値の決定のみ。

---

## Q-10. デプロイ先と運用体制【優先度: 低】

- オンプレミスか、プライベートクラウド（AWS / Azure / GCP）か
- Docker Compose での単一ホスト運用で足りるか、Kubernetes が前提か
- 運用担当（監視・バックアップ・アップデート）はどのチームか

対象 1 リポジトリ・利用者数名の規模であれば、
**Docker Compose による単一ホスト構成**で十分と考えています
（PostgreSQL 1 コンテナ + アプリ 1 コンテナ）。

---

## Q-14. 将来マージブロックへ進む場合の GitHub プラン【優先度: 低】

D-4 のとおり本フェーズではマージをブロックしませんが、将来 Phase 4 で
必須チェック化する場合、**GitHub Free ではプライベートリポジトリの保護ブランチが
利用できません**（GitHub Pro 以上が必要です）。

現時点で対応は不要ですが、Phase 4 を検討する際の前提としてここに記録します。

---

## 次のステップ

優先度「高」の未決事項は残っていません。要件定義と基本設計は完了し、実装の状況は [実装状況](../status.md) にまとめています。

1. Q-6 / Q-8（PIT の実行時間・是正プロセス）を運用開始までに確定させる（Q-7 は D-22 で解消）
2. Q-9 / Q-10（保持期間・デプロイ先）を本番導入前に確定させる
