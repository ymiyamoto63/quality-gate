# like-chatgpt の計測プロファイル（docs/operations/collector.md）
# 「どう測るか」だけを書く。合格ラインは quality-gate の画面（S-06）で設定する。
# KEY=VALUE の形式のみ。シェルとしては実行しない（値の引用符は外して読む）。

QG_REPOSITORY=ymiyamoto63/like-chatgpt
DEFAULT_BRANCH=main
# quality-gate の Actions Secrets のうち、このリポジトリの Ingest Token を入れたものの名前
INGEST_TOKEN_SECRET=QG_INGEST_TOKEN_LIKE_CHATGPT

# バックエンド（Maven）。空にするとバックエンドの計測（M-01 Java / M-07 Java / M-08 / M-11 / M-12）を行わない
BACKEND_DIR=backend
JAVA_VERSION=21
# M-08 に使う JUnit XML（BACKEND_DIR/target からの相対）。API を MockMvc で検証するテストだけを送る
CONTRACT_TEST_REPORTS=surefire-reports/TEST-*ControllerTest.xml
# M-11 / M-12 に使う JUnit XML（BACKEND_DIR/target からの相対、空白区切り）。すべてのテストを送る。
# 省略時は surefire-reports/TEST-*.xml と failsafe-reports/TEST-*.xml
TEST_REPORTS=surefire-reports/TEST-*.xml failsafe-reports/TEST-*.xml

# M-02（PIT）の対象。空にすると M-02 を計測しない。PIT の引数と同じ書き方で、空白区切り。
# PR 以外の計測で全量を実行し、PR の計測では M-02 のスキップを申告する
# （合格ライン（*.gate.yml）の execution.skippable_metrics に mutation_score が必要）。
# 対象の pom の -P mutation と同じ範囲にしている
MUTATION_TARGET_CLASSES=com.example.chatbackend.*
MUTATION_TARGET_TESTS=com.example.chatbackend.*
MUTATION_EXCLUDED_CLASSES=com.example.chatbackend.ChatBackendApplication com.example.chatbackend.config.*
MUTATION_EXCLUDED_TESTS=com.example.chatbackend.*IT
MUTATION_THREADS=2

# M-09 の OpenAPI 定義（リポジトリ相対）。コミットされている定義を比較する
OPENAPI_PATH=api/openapi.yml

# フロントエンド（npm + Vitest）。空にするとフロントエンドの計測（M-01 TS / M-07 TS / M-11 / M-12）を行わない
FRONTEND_DIR=frontend
NODE_VERSION_FILE=.nvmrc
# カバレッジの分母に含めるファイル（FRONTEND_DIR 相対、空白区切り。{ts,vue} のようにカンマを含むため）。
# 指定しないとテストが触れたファイルだけの数字になり、実態より良く見える
FRONTEND_COVERAGE_INCLUDE=src/**/*.{ts,vue}
FRONTEND_COVERAGE_EXCLUDE=src/**/*.spec.ts src/main.ts src/**/*.d.ts
# M-07（フロントエンド）で解析するディレクトリ（FRONTEND_DIR 相対、空白区切り）と、解析しないファイル。
# ESLint の設定と版は quality-gate 側のもの（collector/complexity）。省略時は src と、テストと型定義を除外
FRONTEND_COMPLEXITY_SOURCES=src
FRONTEND_COMPLEXITY_EXCLUDE=**/*.spec.ts **/*.test.ts **/*.d.ts

# 計測の隔離。container（既定）は計測用のコンテナの中で計測する。none はランナー上で直接計測する
ISOLATION=container

# M-10（アクセシビリティ）。空にすると M-10 を計測しない。
# バックエンドの jar と、vite build した画面（vite preview）を起動し、ここに書いた画面をライト・ダークで検査する。
# 検査する画面のパス（空白区切り）。合格ライン（*.gate.yml）の accessibility.pages と一致させる
A11Y_PAGES=/
# 描画が済んだと判断できる要素（CSS セレクタ）。対象の e2e が待つ見出しと同じ
A11Y_READY_SELECTOR=h1
# バックエンドを起動するポート。対象の vite.config.ts の /api の proxy 先（localhost:8080）に合わせる
A11Y_BACKEND_PORT=8080

# M-03〜05（性能）。空にすると性能を計測しない。
# バックエンドの jar を起動し、collector/targets/ にある k6 のシナリオで負荷をかける。
# PR 以外の計測で実行し、PR の計測では M-03〜05 のスキップを申告する
# （合格ライン（*.gate.yml）の execution.skippable_metrics に performance が必要）。
PERF_SCRIPT=ymiyamoto63__like-chatgpt.k6.js
# バックエンドを起動するポート
PERF_BACKEND_PORT=8080
# 計測環境の名前。トレンドの系列を分ける軸になる。ランナーのマシンや計測条件を変えたら名前も変える
PERF_ENVIRONMENT=collector-container
# シードデータの名前（like-chatgpt はメモリ上の固定データだけで動く）
PERF_DATASET_PROFILE=in-memory-fixed
# 実行回数（中央値で判定する。3 回未満は WARN）
PERF_RUNS=3

# 参考値の指標（M-15〜M-17）。合格ラインを持たず、値とトレンドだけを残す（合否には影響しない）。
# 合格ライン（*.gate.yml）で duplication / lighthouse / bundle_size を有効にしたときだけ Run 詳細に出る。
# M-15: jscpd で backend（src/main/java）と frontend（FRONTEND_COMPLEXITY_SOURCES）のコードの重複を数える
DUPLICATION=true
# M-17: vite build した画面の JavaScript と CSS のサイズ（gzip 後）を数える。ビルド結果は FRONTEND_DIR/dist
BUNDLE_SIZE=true
# M-16: Lighthouse で計測する画面のパス（空白区切り）。空にすると計測しない。
# M-10 と同じく、バックエンドの jar と vite preview を起動した上で計測する。画面ごとに LIGHTHOUSE_RUNS 回（既定 3）の中央値
LIGHTHOUSE_PAGES=/
LIGHTHOUSE_RUNS=3
# desktop（既定）か mobile。mobile はモバイルの回線・端末の条件で計測する
LIGHTHOUSE_PRESET=desktop

# 常にスキップを申告する指標（空白区切り）。PR での M-02 のように、計測の都合で申告するものは measure.sh が加える。
# 申告できるのは合格ライン（*.gate.yml）で skippable_metrics に含めた指標だけ。
# 無効（enabled: false）にした指標はここに書かない
SKIP_METRICS=
