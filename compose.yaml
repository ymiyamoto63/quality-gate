# 開発時は `docker compose up db` で PostgreSQL のみを起動する。
# アプリもコンテナで動かす場合は `docker compose --profile full up` とする。
services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: qualitygate
      POSTGRES_USER: qualitygate
      POSTGRES_PASSWORD: qualitygate
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U qualitygate -d qualitygate"]
      interval: 5s
      timeout: 3s
      retries: 10

  app:
    build:
      context: .
      dockerfile: Dockerfile
    profiles: ["full"]
    depends_on:
      db:
        condition: service_healthy
    environment:
      QG_DB_URL: jdbc:postgresql://db:5432/qualitygate
      QG_DB_USERNAME: qualitygate
      QG_DB_PASSWORD: qualitygate
      QG_ARTIFACT_ROOT: /var/lib/quality-gate/artifacts
      QG_GITHUB_CLIENT_ID: ${QG_GITHUB_CLIENT_ID:-}
      QG_GITHUB_CLIENT_SECRET: ${QG_GITHUB_CLIENT_SECRET:-}
      # 収集ランナーが取り込み API に送るトークン（収集ランナーの Secrets の QG_INGEST_TOKEN と同じ値）
      QG_INGEST_TOKEN: ${QG_INGEST_TOKEN:-}
      # データ保持期間（日）。既定は Run 730 / 成果物 90 / 監査ログ 730
      QG_RETENTION_RUN_DAYS: ${QG_RETENTION_RUN_DAYS:-730}
      QG_RETENTION_ARTIFACT_DAYS: ${QG_RETENTION_ARTIFACT_DAYS:-90}
      QG_RETENTION_AUDIT_LOG_DAYS: ${QG_RETENTION_AUDIT_LOG_DAYS:-730}
      # コンテナで動かすときは JSON 構造化ログにする（ログ基盤で requestId / runId を検索できるように）
      QG_LOG_FORMAT: ${QG_LOG_FORMAT:-ecs}
    ports:
      - "8080:8080"
    volumes:
      - ./data/artifacts:/var/lib/quality-gate/artifacts
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 3s
      retries: 12

volumes:
  pgdata:
